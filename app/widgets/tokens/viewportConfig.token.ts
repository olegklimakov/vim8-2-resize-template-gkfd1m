import { InjectionToken } from '@angular/core';
// import { IConfig } from './../models/viewport'

/**
 * token to provide host for http-request
 */
export const ViewPortConfig = new InjectionToken<any>('ViewPortConfig');